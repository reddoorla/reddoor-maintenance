---
"@reddoorla/maintenance": minor
---

feat(dashboard): retire the per-site token model — the operator password gates `/s/<slug>` and `/`. `verifyDashboardToken` is removed; `dashboardToken` is now a fleet-homepage visibility flag only.
