---
"@reddoorla/maintenance": patch
---

Email footers: the first contact line ("Just hit reply.") now renders as a red bold heading, matching the "questions, concerns or requests?" title directly above it — across all three report email types (announcement, launch, maintenance). Previously it was plain black body text, so the call to action read as a quieter footnote than the question prompting it. Following contact lines (e.g. "We're here to help in any way we can.") are unchanged.
