# Morning brief — 2026-06-30

> Written evening of 2026-06-29 via the `evening-review` skill. Scope: **whole repo**. Threshold: **MEDIUM + LOW**. Hunch: **Turso/libSQL data path**. Read-only run — no commits, PRs, pushes, or live-service writes. Method: git archaeology (14 days) + graded the 06-23 brief against current code + a 5-agent area fan-out (Turso · dashboard/cockpit · audits+security+crons · reports+email · recipes/CLI) with every finding traced to file:line and an "investigated — not a bug" pass + my own doc-drift sweep. Local gate state on this exact SHA (`8813bb2`, main): **lint ✓ · typecheck ✓ · test 1791 ✓ · build ✓ · test:dist ✓ (smoke-dist 0.64.0)**. `pnpm audit` = 6 advisories, all the same dev/CI-only chain as 06-23 (none in the request path).

## One-line verdict

Today shipped the cockpit accepted-Watch-conditions feature (#329, merged + rolled out live to 7 sites) on top of a clean green tree — but the review found **two HIGHs and a unifying theme you can't see in any one diff: the system's failure boundaries are mis-calibrated in both directions.** Three separate paths silently convert a *transient upstream failure* into a definitive "all-clear" (a Netlify API hiccup erases a genuinely-broken site from the cockpit's Broken band; an announcement-time GA outage hides the analytics block with no signal; `self-updating` treats a present-but-stale config as "already done"), while the fleet recipe loops do the **opposite** — one dirty checkout throws and silently aborts the rest of the run. No CRITICAL, no data loss, no secret leak. The cockpit/auth/escaping/DB-atomicity code is genuinely well-built; tonight's signal is in the failure paths, not the happy paths.

---

## Top of stack (do these first)

1. **Stop a Netlify API hiccup from clearing a real "deploy errored" alarm (~25 min, [src/audits/netlify-deploy.ts:43-89](../../src/audits/netlify-deploy.ts#L43) + [src/reports/airtable/websites.ts:493-505](../../src/reports/airtable/websites.ts#L493)).** See HIGH-1. `netlifyDeployFields` writes `"Deploy status": null` **unconditionally** on a fetch failure, and `null` drops the site out of the cockpit's Broken band ([fleet-cockpit.ts:82](../../src/dashboard/fleet-cockpit.ts#L82)). A site whose latest deploy genuinely `error`ed silently goes "All clear" on the next sweep that hits one Netlify 500/timeout. The fix is the one-insight change: an **alarm** field must preserve its prior value on a *read failure* (unlike the cert-days *pass-gate*, which correctly clears) — make `hasNetlifyDeployResult` false when the fetch threw, so the prior `error` survives.

2. **Isolate per-site failures in the recipe fleet loops (~30 min, one shared helper, [src/cli/commands/self-updating.ts:52](../../src/cli/commands/self-updating.ts#L52) + 4 siblings).** See HIGH-2. `sync-configs`, `self-updating`, `onboard`, `convert-to-pnpm`, `bump-deps` all loop `results.push(await recipe(s))` with **no per-site try/catch**, and the recipes throw on a dirty checkout. One site with a dirty tree aborts the entire `--fleet` run and every later site is silently never processed. This is the *exact* class `prepareFleetSites` fixed one layer down — add a `runRecipeOverSites(sites, fn)` that settles each into a `failed` RecipeResult, fixing all 5 call sites at once.

3. **Document the dashboard's real deploy env (~15 min, [README.md:355-362](../../README.md#L355)).** See MEDIUM-D. The deployed functions read `DASHBOARD_PASSWORD` (the auth gate), `RENOVATE_TOKEN` + `GH_TOKEN` (the Trigger-Renovate / fleet-action buttons), and `DASHBOARD_BASE_URL` — **none** are in the README env table. A by-the-book deploy yields a dashboard you can't authenticate to and whose action buttons 502. Add the four rows; also add `TURSO_DATABASE_URL` presence to the health-endpoint env report (DOC-2).

---

## Findings — CRITICAL

**None.** No data-loss path, no secret leak, no externally-exploitable auth hole. The dashboard's six state-changing endpoints all gate identically (method → CSRF → password-presence → constant-time `verifyBasicAuth` → env guards → work); every HTML interpolation of site/submission/Airtable data is `escapeHtml`'d; no request-path code shells out (the one `makeGitHub` reference is a stale *comment*); the spam counters are genuinely atomic (`INSERT … ON CONFLICT DO UPDATE SET col = col + 1`). These were specifically probed and held.

## Findings — HIGH

**HIGH-1 — A transient Netlify API failure silently clears a real "deploy errored" alarm from the cockpit's Broken band.** [src/audits/netlify-deploy.ts:60-71](../../src/audits/netlify-deploy.ts#L60) (all failures → `state: null`) → [netlify-deploy-airtable.ts:14-18](../../src/audits/netlify-deploy-airtable.ts#L14) (`hasNetlifyDeployResult` true on any string `checkedAt`) → [websites.ts:493-505](../../src/reports/airtable/websites.ts#L493) (writes `"Deploy status": null` **unconditionally**) → [fleet-cockpit.ts:82](../../src/dashboard/fleet-cockpit.ts#L82) (`isFailedDeployStatus(deployStatus)` drives the Broken band). `fetchLatestProductionDeploy` returns `null` on *any* failure — network error, 429 rate-limit, 500, malformed JSON — and a null fetch is **indistinguishable from "no deploy found"**. So a site whose latest production deploy genuinely `error`ed is surfaced as Broken tonight, but on the next nightly sweep, if Netlify's API hiccups once for that site during the ~10-site sequential loop, `deployStatus` is overwritten `"error"` → `null`, `isFailedDeployStatus(null)` is false, and the broken site silently drops out of the Broken band while production is still down. The "write unconditionally" comment ([websites.ts:494-498](../../src/reports/airtable/websites.ts#L494)) only reasons about the *stale-`ready`* direction; the symmetric case is worse and unhandled. **The insight:** a *pass-gate* signal (cert-days) should clear-to-null on uncertainty (conservative — don't tick pass), but an *alarm* signal (deploy status) must do the opposite — preserve the prior alarm on a read failure. "Mirror domainFields" was the wrong instinct here. **Fix:** make `fetchLatestProductionDeploy` return a discriminated `{ ok: false }` on fetch/parse failure and, on `ok:false`, return a `netlify-deploy` result with **no details** so `hasNetlifyDeployResult` is false and the prior value is left untouched (exactly how a `security` fetch failure falls through to fallback without stamping). _(Rated HIGH for alarm-integrity — it re-introduces the "broken site hides under all-clear" failure mode the three-band cockpit work just closed. Mitigation: it self-heals on the next successful sweep and needs a coincident per-site API failure, so the hide window is ≤1 cycle — re-rate to MEDIUM if that bound is acceptable. Confidence: high, full chain traced + the unconditional null write verified directly.)_

**HIGH-2 — Recipe fleet commands abort the whole batch when one site throws (sibling-isolation gap, 5 commands).** [self-updating.ts:52](../../src/cli/commands/self-updating.ts#L52), [sync-configs.ts:106](../../src/cli/commands/sync-configs.ts#L106), [onboard.ts:59-60](../../src/cli/commands/onboard.ts#L59), [convert-to-pnpm.ts:41](../../src/cli/commands/convert-to-pnpm.ts#L41), [bump-deps.ts:51](../../src/cli/commands/bump-deps.ts#L51). Each is a bare `for (const s of sites) results.push(await recipe(s))` with **no try/catch** around the recipe call. The recipes are not throw-free: `withRecipe` throws on a non-clean working tree ([_with-recipe.ts:62-64](../../src/recipes/_with-recipe.ts#L62), [:87-89](../../src/recipes/_with-recipe.ts#L87)) and re-throws body exceptions ([:188-189](../../src/recipes/_with-recipe.ts#L188)); `bump-deps` (`checkTreeFirst:true`) throws on a dirty tree before any work; `selfUpdating` can throw out of `isWorkingTreeClean`/`createBranch` before its internal try. So in a `--fleet` run, the *first* site with a dirty checkout or a transient git error throws out of the loop and **every subsequent site is silently never processed** — the operator sees a single crash, not a per-site report. This is precisely the class prior reviews fixed for `prepareFleetSites` (clone/prep isolation), just one layer up at recipe-execution; the `audit` command uses the correct isolated `Promise.all` + per-site settle ([audit.ts:129-138](../../src/cli/commands/audit.ts#L129)) and is immune. **Fix:** a shared `runRecipeOverSites(sites, fn)` that wraps each call in try/catch → `failed` RecipeResult (the type already supports it), mirroring `prepareFleetSites`. Fixes all 5 at once. _(Confidence: high — throw paths traced; all 5 loops confirmed un-guarded; the hardened sibling confirmed as contrast.)_

## Findings — MEDIUM

**MEDIUM-A — "Marked spam" counter double-counts when an operator re-marks a submission.** [src/dashboard/submission-status.ts:35-43](../../src/dashboard/submission-status.ts#L35) + [src/db/screenouts.ts:30](../../src/db/screenouts.ts#L30). The status enum is `["new","read","archived","spam"]`, so a row can move spam → read → spam. The only dedup is `row.status === requested → noop`; every *transition into* spam fires `recordMarkedSpam`, which is increment-only (`marked_spam = marked_spam + 1`) with no offsetting decrement and no per-submission idempotency. Toggling one submission spam→new→spam inflates the site/day "got through, marked spam" tally to 2; over time the counter can exceed the number of distinct spam submissions, silently overstating spam-through volume on the cockpit and per-site dashboard. The screenouts test ([screenouts.test.ts:34-37](../../tests/db/screenouts.test.ts#L34)) even asserts two calls → `markedSpam: 2`, confirming no per-submission dedup by design. **Fix:** make it idempotent per submission — stamp `marked_spam_at` on the row and only count the first transition, or derive the tally via `COUNT(*) WHERE status='spam'` instead of a mutable counter. At minimum, rename it to "spam-marking events." _(Confidence: high — full chain traced. This was the Turso-hunch's payoff.)_

**MEDIUM-B — Announcement-time GA/Search soft-fail is silently swallowed → analytics block disappears with no operator signal.** [src/recipes/announce.ts:84-86](../../src/recipes/announce.ts#L84) (reads only `.value`, discards `.softFailed`), contrast [src/reports/draft.ts](../../src/reports/draft.ts) (`updateAnalyticsHealth`) + [src/alerts/digest-collectors.ts:230](../../src/alerts/digest-collectors.ts#L230) (`collectAnalyticsFailures`). This is the #320 "no-data-vs-failure" trap, but in the announcement path. In the regular `--due` draft, a GA *error* sets `analyticsSoftFailAt` on the Websites row → a cockpit/digest "GA/Search enrichment failing" alert. The `announce` recipe never calls `updateAnalyticsHealth`, so if GA has a transient outage during the monthly announcement run, the announcement email's analytics block is hidden (reads as "no GA configured / all good"), the operator gets **zero** signal, and the client receives a one-time onboarding email with the traffic section silently missing. **Fix:** capture the `Enrichment` (not just `.value`) and, when `softFailed` on a configured site, call `updateAnalyticsHealth(base, w.id, now)` best-effort (clear to `null` on clean enrichment), mirroring `draft.ts`'s guard. _(Confidence: high — both paths read; grep confirms no soft-fail handling in `announce.ts`.)_

**MEDIUM-C — `self-updating` never corrects a *stale* CI/Renovate config (present-but-wrong is invisible).** [src/recipes/self-updating/index.ts:117-118](../../src/recipes/self-updating/index.ts#L117). The gate is **existence-only**: `if (present.length < paths.length)` opens the bootstrap PR only when `ci.yml` / `renovate.yml` / `renovate.json` is *missing* on the default branch. If all three exist but their *contents* are stale, it reports `noop: already self-updating` and writes nothing — forever. This is a live trap given this fleet's own history: the Renovate schedule-window bug (per memory, all fleet auto-updates were silently dead for months because of a stale `renovate.json`/cron mismatch) is exactly the drift this recipe is positioned to fix but structurally cannot. The pinned reusable-workflow SHA (`…@78c4da6 # v1.0.0`) is likewise frozen on every bootstrapped repo. Content-diffing exists only in the *separate* `sync-configs` recipe ([sync-configs.ts:91-92](../../src/cli/commands/sync-configs.ts#L91)), which isn't wired into any nightly workflow. **Fix:** either have `self-updating` content-diff the three templates (reuse `sync-configs`'s `existing === t.contents` check) and refresh the PR on drift via `findOpenSelfUpdatingPR` (no duplicate stacking), or explicitly wire `sync-configs` into the nightly sweep and document drift-repair as its job. _(Confidence: high on mechanism, medium on severity — silent staleness, not data loss, but it directly undercuts the auto-maintenance premise and has bitten this fleet before.)_

**MEDIUM-D — Deploy docs never learned the dashboard's real env; a by-the-book deploy yields an unauthable dashboard with dead action buttons.** [README.md:355-362](../../README.md#L355) env table. The deployed functions read `DASHBOARD_PASSWORD` (the operator auth gate), `RENOVATE_TOKEN` + `GH_TOKEN` (the Trigger-Renovate and fleet-action buttons — request-path GitHub REST), and `DASHBOARD_BASE_URL` (link building) — verified via `git grep process.env` across `netlify/**` + `src/dashboard/**` + `src/forms/**`. **None** appear in the README. The table lists only `AIRTABLE_*`, `RESEND_WEBHOOK_SECRET`, `TURSO_*`, `FORMS_INGEST_TOKEN`. So a fresh deploy from the docs can't authenticate the operator and its action buttons 502. **Fix:** add the four rows (note `DASHBOARD_PASSWORD` is the auth gate; `RENOVATE_TOKEN` needs `actions:write`). Pairs with DOC-2 below. _(Confidence: high — env-var names cross-checked code-vs-docs directly.)_

## Findings — LOW

- **LOW-1 — Cockpit verdict "healthy" count undercounts by one per hidden-site pending report.** [fleet-render.ts:135-138](../../src/dashboard/fleet-render.ts#L135) + [fleet-cockpit.ts:401-416](../../src/dashboard/fleet-cockpit.ts#L401). `healthy = Math.max(0, total - (broken+watch+approval))` with `total = visible cards`, but `pending` is resolved against *all* websites — an approval on a hidden site (status not maintenance/launch-period) subtracts from `total` though it never counted toward it. The `Math.max(0,…)` clamp prevents a negative; worst case is a cosmetic off-by-N in the "N healthy" meta-line. Compute healthy from cards only.
- **LOW-2 — Netlify-deploy `fetch` has no `AbortSignal` timeout.** [netlify-deploy.ts:103-106](../../src/audits/netlify-deploy.ts#L103). Unlike the Dependabot fetch (`AbortSignal.timeout(15s)`), a half-open TCP could stall the ~10-site sequential sweep; backstopped by the 75-min job timeout, so latency-risk not hang-forever. Fold an abort signal in alongside the HIGH-1 fix.
- **LOW-3 — Dependabot mid-pagination failure silently falls back to the weaker `pnpm audit`.** [gh-rest.ts:128-166](../../src/github/gh-rest.ts#L128) + [security.ts:216-221](../../src/audits/security.ts#L216). If page 2 of a >100-alert repo returns a 502/secondary-rate-limit, the loop throws, `dependabotAudit` returns `null`, and `securityAudit` falls back to lockfile `pnpm audit --prod` — different (narrower) coverage than the GitHub Advisory path the system now prefers, with no signal that the preferred path degraded. Can't produce a false *tick* (the fallback still gates on a fresh pass), only a missed vuln + invisible degradation. Add one retry-with-backoff honoring `Retry-After`, and/or log a distinct "fell back to pnpm audit" line so a systematic degradation (e.g. token lost `Dependabot alerts: read`) is visible.
- **LOW-4 — Announcement reuse path: the reviewed *preview* HTML can diverge from the *sent* email on a GA soft-fail.** [announce.ts:88-118](../../src/recipes/announce.ts#L88). On a run-2 soft-fail, `updateReportScores`'s `!== undefined` guards correctly preserve the stored GA value (good), but the run-2 preview is rendered from the live (null) `gaUsers` and omits analytics, while the *sent* email reads the stored value and *shows* it. The operator reviews a preview that understates what the client receives. Tie the fix to MEDIUM-B (render the preview from the stored enrichment when the live fetch soft-fails).
- **LOW-5 — `selftest email --all` filters to maintenance sites regardless of `--type`.** [selftest-email.ts:72-79](../../src/recipes/selftest-email.ts#L72). `--all --type launch` renders Launch emails for maintenance-status sites — a self-test of a state that can't happen in production. No send-safety issue (operator-only, no Airtable writes — that part is clean). Widen the filter to the type's natural status set or print a one-line note. Optional.
- **LOW-6 — Spam-counter concurrency test can't prove cross-invocation atomicity.** [screenouts.test.ts:20-28](../../tests/db/screenouts.test.ts#L20). 50 `recordScreenOut` on one shared in-memory client passes trivially (libSQL serializes one connection) but doesn't exercise the real two-Lambda/one-Turso shape. The upsert IS atomic server-side, so the code is correct — only the test's coverage claim is overstated. Rename/comment it, or open two `file:` clients and interleave.
- **LOW-7 — `fleet_events` retention prune is best-effort/swallowed → silent "retention stopped" risk.** [fleet-events-writer.ts:27](../../src/audits/fleet-events-writer.ts#L27) + [fleet-events.ts:84-85](../../src/db/fleet-events.ts#L84). `pruneFleetEvents` shares the inserts' swallowing try/catch; sustained prune-only failures (e.g. a write-permission issue while reads work) would grow the table unbounded with no alarm. Low likelihood/impact (reads bounded at 20 over 7 days). Let the prune failure surface, or emit a distinct greppable log tag — it's in the nightly sweep, not the hot path.
- **LOW-8 — Stale `makeGitHub` comment + unvalidated legacy `gitRepo` in the Renovate dispatch.** [trigger-renovate.ts:3](../../src/dashboard/trigger-renovate.ts#L3). The comment says `makeGitHub`; the `.mts` correctly uses `makeGitHubRest`. `triggerRenovateForSite` dispatches to `site.gitRepo`; the editor validates with `REPO_RE` on write but a *legacy* Airtable value isn't re-validated here. **Not exploitable** (`splitRepo` + `assertUrlSegment` reject malformed/traversal repos → clean 502, bounded to the operator's token). Fix the comment; optionally assert `REPO_RE.test(repo)` in-place.
- **LOW-9 — `convert-to-pnpm` partial-failure test asserts only status, not the restored fs/git state.** [convert-to-pnpm.test.ts:179-193](../../tests/recipes/convert-to-pnpm.test.ts#L179). The recipe is safe *today* (destructive steps are committed on the recipe branch before the failing `pnpm install`, so `restoreAfterFailure` restores them), but the only test asserts `status==='failed'` — not that `package-lock.json` is back, the recipe branch is deleted, and HEAD is on the original branch. A future refactor moving the lockfile `rm` after the install would silently break a site with no test catching it. Add those assertions to lock the invariant.
- **LOW-10 — Health-endpoint env report omits `TURSO_DATABASE_URL` presence.** [README.md:376-380](../../README.md#L376) shows the `resend-webhook` health JSON reporting only `RESEND_WEBHOOK_SECRET`/`AIRTABLE_*`, even though the functions 500 without `TURSO_DATABASE_URL`. Add it to the presence check so the deploy smoke test catches the #1 fresh-deploy failure. (Pairs with MEDIUM-D.)
- **LOW-11 — Dead `playwrightA11yConfig` export, pinned to `localhost:5173`.** [src/configs/playwright-a11y.ts:18](../../src/configs/playwright-a11y.ts#L18). Still exported + tested with no production importer. **Carried from the 06-10 AND 06-23 briefs — now three briefs running.** Confirm no importers, delete it + its test.

## Investigated — NOT a bug (so you don't re-investigate)

- **Dashboard auth & Lambda-safety:** all six state-changing endpoints gate identically (method → CSRF → password-presence → constant-time `verifyBasicAuth` → env guards → work); read pages auth *before* the env guards (no backend-fingerprinting); `verifyBasicAuth` compares byte-lengths first (multibyte-RangeError trap handled) and fails closed. No request-path shell-out anywhere — the lone `makeGitHub` is a stale comment (LOW-8). Site-details editor rejects unknown fields against an allowlist before any read; slug regex + `JSON.stringify` filter is injection-safe; unknown slug → 404 not whole-fleet leak. **Strong.**
- **Spam-counter atomicity & fleet_events idempotency:** the upsert is single-statement `ON CONFLICT DO UPDATE SET col = col + 1` (atomic server-side); `recordFleetEvent` uses `onConflict(id).doNothing()` against a TEXT PRIMARY KEY with deterministic ids (`pr_automerged:repo#n`, `…:ymd`) — no check-then-insert race. Migrations are pure `CREATE … IF NOT EXISTS` (safe under non-transactional `executeMultiple`). `openDb` warm-cache evicts on failure. Kysely types match the DDL column-for-column. **Strong.**
- **Send-path / checklist integrity:** `isChecklistComplete` enforced in *both* `approve.ts` and `sendOne` (backstop even if "Approved to send" is ticked directly in Airtable); `autoTickChecklist` ticks only on `pass` AND `isFresh` (≤3 days) — never on stale/failed evidence; send + digest use stable Resend `Idempotency-Key`s (409 = already-sent); a mid-send failure leaves the row unstamped → replays, no double/lost send; the 06-23 `r.draftReady` pile-up fix is intact. **Strong.**
- **Workflow hardening:** `fleet-security` reached full partial-failure parity with `fleet-lighthouse` (no-summary/`wrote=0`/>25%-failed → exit 1; deduped `if: failure()` tracking issue with a *distinct* title; `timeout-minutes: 30`). `github-signals` total-failure gate + `continue-on-error` is correct. `renovate-dispatch` skips repos with an open Renovate PR (no duplicate dispatch). Least-privilege perms + timeouts on every job. **Strong.**
- **Recipe safety:** all git ops use `execFile("git", [args])` (no shell); repo names/URLs validated (`assertSafeName`/`assertSafeRepoUrl`, `git clone --` separator); `sync-configs` guards its two known clobber-traps (svelte `$utils` alias, netlify security headers) with compliance checks not blind overwrite; `self-updating` restore-on-failure + `prepareFleetSites` isolation both hold. HIGH-2 is the *un-hardened layer above* this, not these.
- **Dependency posture:** `pnpm audit`'s 6 advisories (protobufjs via `@google-analytics`, js-yaml via `@changesets`) are the same dev/CI-only chain triaged 06-23; the request-path Netlify functions pull none of them; `ws` is resolved (gone from the tree since #317).

## Open loops carried forward (graded)

**Closed since 06-23** (✅): that brief was heavily cleared — HIGH-1/2/3 shipped #283, MEDIUM-A–I shipped #285/#286, the LOW sweep shipped #288. LOW-1 (`.prettierignore` briefs) and LOW-9 (README `tucksravin` slug) are both fixed and verified stuck. HIGH-3 (Turso deploy docs) landed — README + `.env.example` now carry `TURSO_*` (MEDIUM-D tonight is the *next* layer: the dashboard-specific vars).

**Still open** (⏳):
- **GA/Search single-subject SPOF** ([src/reports/ga/client.ts:12](../../src/reports/ga/client.ts#L12)) — one impersonated Workspace subject backs every site's analytics; offboard it and all GA/Search blanks at once. **Flagged in the 06-10, 06-23, and now this brief.** Provisional call unchanged: move to a role account (`reports@reddoorla.com`) **before the fleet passes ~25 sites** — it's creeping toward that, and MEDIUM-B shows analytics-failure handling is still being retrofitted path-by-path (another reason to make the source robust).
- **LOW-11 dead `playwrightA11yConfig`** — carried three briefs, ~5-min delete.
- **Branch hygiene** — archaeology found **no orphaned work, no stashes** (good), but ~13 stale local `feat/*` branches (all squash-merge remnants or stale-behind-main, incl. `feat/dependabot-security-ingest` whose feature is already in via #327) + a long tail of stale remote `docs/journal-*`/`chore/*`. A `git fetch --prune` + `git branch -d` sweep is ~10 min; low value but it's what lets a *genuinely* orphaned branch stand out next time (the 2026-05-21 brief caught real orphaned work exactly this way).

## Decisions deferred

_(Things I'd have asked mid-review; each with my provisional call.)_
1. **HIGH-1 severity — HIGH or MEDIUM?** _Provisional: fix at HIGH altitude regardless._ It self-heals next sweep and needs a coincident API failure, so the hide window is ≤1 cycle — but it re-opens the exact "broken hides under all-clear" hole the cockpit rework just closed, and the fix is small. Ship it with HIGH-2 as a paired "failure-boundary" cleanup.
2. **HIGH-2 fix shape — shared helper now, or inline try/catch per command?** _Provisional: shared `runRecipeOverSites` helper._ Five call sites with the identical gap is a DRY signal; one helper mirrors the `prepareFleetSites` pattern you already trust and prevents the 6th command from reintroducing it.
3. **MEDIUM-C — teach `self-updating` to content-diff, or wire `sync-configs` nightly?** _Provisional: content-diff inside `self-updating`._ It's more faithful to the recipe's name and keeps one nightly entry point; reuse `findOpenSelfUpdatingPR` so refreshed PRs don't stack.
4. **MEDIUM-A — fix the counter, or just redefine it?** _Provisional: derive via `COUNT(*) WHERE status='spam'`._ A derived tally can't drift on re-marks and removes the mutable-counter class entirely; the increment path stays only for the screen-out (never-stored) case where there's no row to count.
5. **GA SPOF — act before scaling?** _Provisional: yes, before ~25 sites_ — unchanged from two prior briefs; a ~5-min role-account re-grant that prevents a silent fleet-wide analytics outage.

## What I did NOT do tonight

- **No code changes, commits, PRs, pushes, or live-service writes.** Read-only as contracted. My review ran against main's committed code at `8813bb2`. **Note:** mid-review a *parallel session* switched the checkout to a branch `fix/audit-lazy-libsql` and left uncommitted edits to `src/db/client.ts` + `scripts/smoke-dist.mjs` — those are **not mine**; I left them untouched. They're not material to any finding here (the Turso findings live in `screenouts.ts`/`submission-status.ts`, not `client.ts`). (The earlier-today #329 merge + Airtable accepted-conditions rollout were separate, pre-review tasks completed before the evening review began.)
- **Writes I was pre-cleared for only:** this brief, and the `MORNING_REPORT_2026-06-30.md` path added to `.claude/settings.local.json` (Phase 1b).
- **Local gates:** ran the full suite fresh this time (test 1791 / lint / typecheck / build / test:dist all green on `8813bb2`) plus `pnpm audit`.
- **Did not deeply review:** the Svelte-4→5 codemods, the exact MJML layout internals beyond the escaping/CID pipeline (that pipeline *was* traced), and the Prismic/site-template content. Flagged for a future pass. The five core subsystems (DB, dashboard, audits/crons, reports/email, recipes/CLI) each got a dedicated traced review this round.
- **Did not empirically reproduce** HIGH-1 against the live Netlify API (it's a code-structure certainty — the unconditional null write is verified directly — but the actual API-hiccup event wasn't simulated).
