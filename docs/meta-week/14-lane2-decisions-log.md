# 14 — Lane 2 decisions log (meta week, 2026-09-14)

The running record Lane 2 kept during Monday, committed as written (spec §5, deliverable 7). Numbers and claims are as they were recorded at the time; the journal entry for the day is the narrative.

Running record for the journal and for the operator's five decisions (spec §3).

## Done

- **S1 sharp wave — CLOSED 2026-09-14.** Dependabot `sharp` group: 0 (was 9 on 09-12). 6 of 9 repos cleared by Renovate's own `lock-file-maintenance` in-run merges (17:08–17:46Z); worker merged reddoor-starter#118 (8f73563), gallerysonder#96 (1b64874), msot#63 (921cf24). Override PRs autoclose (msot#64 proof); starter#119 and gallerysonder#97 still OPEN/DIRTY, will autoclose on next Renovate run. Image proof: local build with a throwaway imagetools route (9 derivatives); plain starter build never invokes sharp. Remaining open alerts: 20 (11 high), none `sharp`, none on client repos.
- **Item 1 (checkbox on reddoor-starter #97) — NOT DONE, correctly.** `unschedule-branch` no longer exists; the branch `renovate/all-minor-patch` now sits under "Pending Status Checks" with an `unpend-branch` control. Branch tip fb528b5 (recreated 2026-09-14T02:28Z) has ONE status (`renovate/stability-days` = success) and ZERO check runs — repo CI never runs on it. Worker did not tick a different control than briefed.

## Corrections to the package, measured

- S1: dependency PRs are lockfile-only; "merge A then update-branch B" is refused; override PRs delete a load-bearing comment. Two of three were already in fleet memory (2026-08-03) and did not reach the brief.
- S7's "tick unschedule-branch" is stale as written.

## For the operator

1. `reddoor-starter` `renovate/all-minor-patch` (11 deps incl. svelte, vite, node, @reddoorla/maintenance, typescript-eslint) is stuck "pending status checks" with no PR and no CI runs. Decide: tick `unpend-branch` on #97, or leave for the S7 outcome metric to observe.
2. GCP console: referrer restrictions on the three Maps keys (S3 steps 1–2). Not started; agents cannot do it.

- **S2 — MERGED (PR #777, squash, head 184385ef).** All 24 `gh issue list` sites carry `--search 'in:title "…"'` + `--limit 200`; the protection-gap close loop gates each issue on its own body's GAP set (COVERED lines only, SKIPPED excluded). Gate test `tests/build/tracking-issue-query.test.ts` executes the real step shell against a 40-issue stubbed gh: position-2 control green on old and new text; position-35 case red on old (`STUB_CREATE` = #754 being born), green on new. #652 closed by hand after its three gaps were verified healed live. Finding: #652 and #754 had DISJOINT gap sets — the open/update half comments the current gaps onto an old issue and never rewrites its body (stale-body defect, still open). Tradeoff recorded: `--search` is eventually consistent → a rare false-empty files one duplicate that the next night dedupes.

- **S5 — MERGED (PR #780, squash, head 95ee5e1e).** `fleet-form-e2e` now fails on `wrote=0` and emits `FLEET_FORM_E2E skipped=N total=M` on every run (warning, never a threshold); `release-health`'s two close steps require positive markers (`NPM_DRIFT behind=no`, `RELEASE_STATE healthy=yes`) written only by an observed comparison. Gate tests `tests/build/fleet-form-e2e-workflow.test.ts` (7 cases) and `tests/build/release-health-workflow.test.ts`. Rollout issue #779 names the five uncovered sites (gallerysonder, revogen, erp-industrial, data-dynamiq, la-homelessness-initiative); split 6/7 reproduced by grep. Corrections to S5: the hole was `total=0` only (the mass-flake gate already reds `wrote=0` whenever anything was attempted); release-health guard 1 had the same unset-output defect as guard 2; `${{ }}` in a `run:` block must be substituted before the harness can execute it; both workflows used hard-coded `/tmp` paths (the sandbox denies `/tmp` writes, so the old-text baseline had to run unsandboxed).

- **S3 instrument half — MERGED (PR #781, squash 7e72a052, after update-branch and a re-gate on the new head).** `whsec_` fixture ≠ all five live `RESEND_WEBHOOK_SECRET` context values (live-vs-live control MATCH first) → alert dismissed `used_in_tests` 2026-09-14T18:09Z; no rotation. Secret scanning + push protection enabled on `vida-legacy-foundation` and `29-navy` (both were #754's entire gap set). New third clause `openSecretAlerts` in `secretScanningGaps`: real run `PROTECTION_AUDIT gaps=4 covered=22 skipped=6 total=32`, the four = reddoor-starter, gallerysonder, beachfront-dentistry, reddoor-starter-blux (the open alerts); PASS controls erp-industrial + espada COVERED; unreadable token → `unavailable` → gap, never fine. Heal line in the tracking-issue body now names one heal per surface. Riders: forward pointers on the two blux docs; CLAUDE.md line on `the-tower` (fourth archived repo, invisible to `fleet-repos.sh`). NOT done: `secret_scanning_validity_checks_enabled` — the org API returns 200 and ignores the write on the Free plan (UI or Secret Protection needed); credential-residue rider (operator's file).
- Findings for the operator: `RENOVATE_TOKEN` in `~/.config/reddoor-maint/credentials.env` is dead (401) and `protection-audit` prefers it over `GH_TOKEN` — the locally-run audit only works with `RENOVATE_TOKEN="$(gh auth token)"`; the `GITHUB_TOKEN` that #650 flagged was replaced 2026-09-01 and works. `resolution_comment` on alert dismissal is capped at 280 chars.

- **S4 — DONE to the approval gate (no PR; nothing sent).** Reddoor Maintenance report drafted through the real path: row `recVCP3qWUi71B15H`, "Reddoor — Maintenance — 2026-09-14", `draftReady=true approvedToSend=false deliveryStatus=pending`, all three Turso mirrors OK, body 57,573 bytes identical to the preview. Content clean: six ticked checks, Lighthouse 98/100/100/100, ANALYTICS `15,063 Users ▲ 510% (2,471 → 15,063)` — the 510% is real (six 30-day windows walked). Commentary is null and `Last tested date` is never rendered by any template. Slug is `reddoor` (S4 said `reddoor-website`). Five sites due 2026-10-05: espada (534 users ▼15%) and vineyard (429 ▲3%) resolve; data-dynamiq, revogen, la-homelessness-initiative have blank GA4 = unstarted setup, ANALYTICS absent, no `Maint: Google Indexed` evidence (#469's triple). **All five currently fail the pre-send health gate** (four on `Maint: Security Updates ⇒ fail — 1 high vuln`, LAHI on `Maint: CMS Checked ⇒ unknown`) — that, not GA, is what would leave five rows unsent on 10-05. Findings: `scratchpad/s4-report-proof.md`.
- Defects S4 surfaced: (1) `Analytics soft-fail at` does not exist in Airtable → every real draft throws `Unknown field name` → the Turso mirror after it never writes `site_health.analytics_soft_fail_at` → the digest signal at `digest-collectors.ts:426` can never fire; (2) `report --preview --enrich` discards `softFailures`/`searchDefaultMissed`/`searchPropertyMissing`; (3) the GA4 cell gates Search Console enrolment too (`enrolled = ga4PropertyId || searchQuery`) — one `Search query` cell per row restores the Google-Indexed half without GA; (4) preflight: MSOT and Revogen resolve to the same recipient `accounting@revogenbiologics.com`; (5) `sendOne` value-imports the Airtable client and fetches the header image from an Airtable signed URL — the send path is more Airtable-shaped than #646 assumes.

## For the operator (added Monday evening)

3. **Approve or reject the queued Reddoor report** (row `recVCP3qWUi71B15H`) in the cockpit; it goes to Reddoor's own recipients. Read the 510% line first.
4. **Before 2026-10-05:** the four sites failing on one high vuln each (data-dynamiq, espada, vineyard-custom-homes, revogen) and LAHI's unknown CMS check. Lane 2 can take the vuln clears Tuesday if you say so.
5. **Phase 6 ordering:** 21 days to the five-site batch, 17 to Sonder. S4's recommendation: let them fire on the store the path was written for, then land Phase 6. Needs a dated go either way on #646.
6. **MSOT ↔ Revogen shared recipient** — a MSOT report on 2026-11-21 would reach Revogen's accountant. Fix the cell.

## Open / in flight

- S1–S7 all done as of 2026-09-14 ~20:40Z (S1 closed by Renovate + worker; S2 #777; S3 #781; S4 to the approval gate; S5 #780; S6 #785; S7 #784). Both Lane 2 worktrees removed. Remaining Lane 2 deliverable: the continuity page (07 §2.3), Thursday.
- Follow-ups filed: #779 (testMode rollout), #782 (Analytics soft-fail field), #786 (dead-letter `abandoned` outcome + card-less cockpit lane).

## Operational notes for the journal

- `gh` inside a shell `for` loop fails x509 in the sandbox; sequential works (memory written).
- Sharp worker: 169k subagent tokens, 96 tool uses, 23 min — most of the wave had closed itself before it started; the reading, not the merging, was the value.

- **S7 metric — MERGED (PR #784, squash c1e90b61, head 8c9ae8a9 gated).** `renovateOutcome` on `ProtectionCoverageRow`: days since the repo last MERGED a feature-update Renovate PR (excludes `lock-file-maintenance` and `npm-*-vulnerability` heads), one `pulls?state=closed` call per repo, three states (`delivering` / `drought` / `unmeasured` — never a drought without a baseline). `WARN` line per repo + `RENOVATE_OUTCOME drought=N delivering=N unmeasured=N threshold=21d`; never gaps, never files. Controls from real frozen data (242 merged `renovate/*` heads on 26 repos): FAIL today `drought=21 delivering=0 unmeasured=5`; PASS at 2026-08-10 `drought=0 delivering=20 unmeasured=6`; threshold 21 justified by the empty band 14.1…32.3 days, and 14 would have warned on 2 healthy repos. Monday observation (`_data/renovate-observation-2026-09-14.json`): 26 repos hold `renovate/all-minor-patch`, all PR-less, all tips rewritten 09-14; 123 PR-less `renovate/*` org-wide (103 on 09-07); PR-less branches did NOT convert. Corrections: last grouped merge is 2026-08-13 (beachfront), not 08-12; #97's marker is `unpend-branch`, nothing ticked. Mechanism still unresolved: Renovate says "pending status checks", GitHub says combined status `success` with 0 check runs. Honest accounting: `pnpm test` was green while `tsc` failed (vitest does not typecheck) — fixed in the second commit.

- **S6 — MERGED (PR #785, squash 2694cc04, head bf970a8b gated after update-branch).** Latent, not live: all five `testMode` sites have a Turso row; production `submission_deadletter` = 0 rows. `/health`: 29-navy and hedloc declare `ingestUrl`+`ingestToken` (NOT vida + 29-navy as the package said); the-pointe-burbank and vida-legacy-foundation have no reachable deployment. Items: (1) `ensure-site` `exists` branch heals a missing Turso row (`healedDbRow`, loud line); (2) `ingest.ts` unknown-site → dead-letter row via the one `captureDeadLetter` writer, status stays 404; (3) `replay-deadletters` resolves through `makeLazySiteLookup` (Turso first; Airtable opened UNCALLED under the freeze — a missing PAT no longer refuses the replay); (4) `deadletter` attention kind, cockpit + digest wiring, card-less items reach the digest as `(unknown site: <slug>)`. RED→GREEN per item, nine mutation checks all red, round trip PASSED against a libSQL `file:` DB through vitest (`turso dev` cannot bind in the sandbox). `pnpm test` 6583 passed. Design consequence: `unknown-site` no longer terminal on replay — accepted (correctness over convenience); the terminal `abandoned` outcome and a fleet-level cockpit lane filed as #786. Corrections: no `db row`/`db sites` read CLI exists; line numbers exact.
